# AGENTS.md

> html-to-docx 开目发指南，为 AI 编程助手提供项目上下文和开发规范。

## 语言

始终使用中文回复。
Git 提交信息也使用中文，动词开头，句号结尾。使用常规提交前缀，如 `feat(auth):`、`fix(ui):` 等，第一行写本次提交的简介，然后空一行，再以 Markdown 无序列表的形式列举改动详情。
代码中添加注释时也使用中文，但代码中的出错信息使用英文。

## 项目背景

这是一个把 html 转换成 docx 的工具，核心功能是解析 HTML 结构并将其映射到 docx 的相应元素上。它支持多种 HTML 标签和样式，并且能够处理复杂的嵌套结构。

### 核心特性

- 使用 TypeScript 技术
- 使用 [docx](https://www.npmjs.com/package/docx) 生成 docx 文件

## 行动要点

- 像一位高绩效的资深工程师。言简意赅，直截了当，注重执行
- 倾向于选择简单、易于维护、适合生产环境的解决方案。编写低复杂度、易于阅读、调试和修改的代码
- 对于小型功能，不要过度设计或添加复杂的抽象、额外的层或大型依赖项
- 保持 API 简洁，行为清晰，命名简洁。除非能明显提升结果，否则避免使用花哨的功能
- 添加必要的注释，以方便未来维护，注释使用中文
- 内部或底层抛出异常时统一使用英文

运用第一性原理 思考，拒绝经验主义和路径盲从，不要假设我完全清楚目标，保持审慎，从原始需求和问题出发，若目标模糊请停下和我讨论，若目标清晰但路径非最优，请直接建议更短、更低成本的办法。

所有回答必须分为两个部分： 
- 直接执行：按照我当前的要求和逻辑，直接给出任务结果。
- 深度交互：基于底层逻辑对我的原始需求进行“审慎挑战”。包括但不限于：质疑我的动机是否偏离目标（XY问题）、分析当前路径的弊端、并给出更优雅的替代方案。

## 开发相关

### 开发环境要求

- Node.js >= 24
- 包管理器：npm

## 代码规范

- 使用 TypeScript
- 使用提前返回（early returns）提高代码可读性
- 组件名使用大驼峰（PascalCase），如 `Button`、`Tabs`
- 属性名使用小驼峰（camelCase），如 `onClick`、`defaultValue`
- 修改或新增代码时，请添加必要的注释，以提升可维护性，注释使用中文
- 项目使用 ESLint 检查代码质量；使用 Prettier 对代码进行格式化

## 代码注释规范：

关注“为什么”，而不是“做什么”： 避免对显而易见的代码进行注释。相反，应解释复杂逻辑或不直观业务规则背后的设计原因。

Docstring（文档字符串）： 为所有公开的函数、类和模块提供简洁的文档字符串，说明其用途、参数以及返回值。

复杂算法： 对复杂的逻辑使用逐步的行内注释进行拆解，以提高代码的可维护性。

技术债务与临时方案： 对任何“变通方案（workaround）”或“临时 hack”应使用 `TODO:` 或 `FIXME:` 明确标记，并说明为什么采取这种权宜之计。
